# Authentication Feature - Data Model

The Authentication feature relies on the `User` data model, which is defined in the main Backend API data model.

Please refer to the [`app/backend-api/specs/data-model.md`](app/backend-api/specs/data-model.md) file for the definition of the `User` model.