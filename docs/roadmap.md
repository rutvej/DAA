# Roadmap (Known Gaps)

The items below are known gaps or inconsistencies to address in future iterations:
- [x] Add `GET /dashboard` endpoint (admin panel expects it) — **Completed**.
- [x] Align auth enforcement across log endpoints (list vs submit) — **Completed**.
- Update log status when python-agent updates fix status.
- Consolidate environment variable usage across services.
- Improve observability (structured logging, tracing, metrics).
- Add pagination + filtering UX to admin panel (search UI is present but not wired).
